# Saha Notes Beta — public test repository

Synthetic, disposable test data only. No personal or work information.

Use branch `main` and folder `notes` for the initial import test. This repository tests public repository discovery, authorization, and import. It is not a production workspace.

Expected initial selection: two Markdown files under `notes`, including one nested folder. Edit, refresh, and disconnect checks must preserve local notes. Delete this repository only after a deliberate cleanup decision.
